on: 
  push:
    branches:
      - develop
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: "ftp.robertochoaweb.com"
        username: "u125968231.ccontrol"
        password: "S4brinaRobert80!"
        log-level: verbose
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**